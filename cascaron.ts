// MAIN.TS // npm i  class-transformer class-validator pg typeorm @nest/typeorm uuid @nestjs/mapped-types

/*
app.enableCors();

app.setGlobalPrefix('api');

app.useGlobalPipes(
    new ValidationPipe({
        whitelist: true,
        forbidNonWhitelisted: true,
    }),
);
*/

// APP.MODULE.TS //

/*ConfigModule.forRoot(), npm i @nestjs/config
    

    TypeOrmModule.forRoot({

      // ssl: process.env.STAGE === 'prod' ? true : false,
      // extra: {
      //   ssl: process.env.STAGE === 'prod' ? { rejectUnauthorized: false } : null
      // },
      type: 'postgres',
      host: process.env.DB_HOST,
      port: +process.env.DB_PORT,
      database: process.env.DB_NAME,
      username: process.env.DB_USER,
      password: process.env.DB_PASSWORD,
      autoLoadEntities: true, // para que cargue auto las entidades que vamos creando
      synchronize: true, // en produccion no se usa
    }),
*/

// COMMOM MODULE //

/*
- Pagination DTO
- HandleExceptionService

*/

// CLOUDINARY MODULE // npm i cloudinary


// AUTH // 


// npm i bcrypt
// npm install  @nestjs/passport passport 
// npm install  @nestjs/jwt passport-jwt

/// Seed Module ///
/// Auth Module ///
/// Users Modules ///




